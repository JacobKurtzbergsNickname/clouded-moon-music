Implement the next unfinished requirement from docs/production-requirements.md.

Steps:
1. Read docs/production-requirements.md
2. Check git log and source files to determine which requirements are already implemented
3. Pick the highest-priority unimplemented item (P0 first, then P1, then P2, then P3)
4. Implement it fully — write the code, add tests, update any relevant docs
5. Run `npm run lint` and `npm test` to verify nothing is broken
6. Commit with a message referencing the requirement ID (e.g. "feat: add global exception filter (ERR-1)")
7. Report which requirement was completed and which is next
